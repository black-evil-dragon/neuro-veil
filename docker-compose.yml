version: '3.8'
services:
  app:
    build: ./src
    command: python main.py
    volumes:
      - .:/app
      - ./output:/app/output
    working_dir: /app
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility